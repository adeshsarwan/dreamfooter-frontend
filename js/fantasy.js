async function boot(){ const d=await dfFetch('/api/public/fantasy/pool'); cards.innerHTML=(d.data||[]).map(c=>`<div class="team-card"><div class="flex items-center justify-between"><b class="text-3xl">${c.base_stat||80}</b><span class="pill">${c.rarity||'Card'}</span></div><div class="mt-4 flex items-center gap-4"><div class="crest">${c.player_image?`<img src="${c.player_image}">`:'⚽'}</div><div><h2 class="text-lg font-black">${c.player_name}</h2><p class="muted">${c.team_name||''} · ${c.position||c.card_type||''}</p></div></div><button class="btn btn-ghost mt-4 w-full">Add to Squad</button></div>`).join('')||'<div class="panel muted">No cards generated yet. Run rebuild/generate cards.</div>';}
boot().catch(console.error);
