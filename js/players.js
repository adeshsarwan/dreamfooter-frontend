const box=document.getElementById('players'), q=document.getElementById('q');let timer;
async function load(){ const term=q.value.trim(); const data=await dfFetch(`/api/public/players${term?`?q=${encodeURIComponent(term)}`:''}`); box.innerHTML=(data.data||[]).map(p=>`<a class="player-card2" href="/player.html?id=${p.sportmonks_player_id||p.id}"><div class="flex items-center gap-4"><div class="crest">${p.image_url?`<img src="${p.image_url}">`:'⚽'}</div><div><h2 class="text-lg font-black">${p.display_name||p.common_name}</h2><p class="muted text-sm">${p.position||''} ${p.nationality?`· ${p.nationality}`:''}</p></div></div></a>`).join('')||'<div class="panel muted">No players found.</div>';}
q.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(load,250)});load().catch(console.error);
